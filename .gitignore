# Local data stores — never committed (§15.3)
#
# `recordings/` now holds narration audio (recordings/<id>/audio.webm) as well
# as snapshots, so this covers a tester's actual voice. Do not narrow it to
# un-ignore "just the json": the audio sits in the same directory and is the
# one artifact here that is a person rather than a page.
recordings/
runs/

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node
node_modules/
.pnpm-store/
*.tsbuildinfo

# Build output
extension/dist/
ui/dist/
fixtures/demo-app/dist/

# Local config overrides (may contain API keys)
*.local.yaml
.env
.env.*
!.env.example

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Playwright
test-results/
playwright-report/
.playwright-profile/
