{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aitc-rem.local/schema/ir.schema.json",
  "title": "IRDocument",
  "description": "SS10 -- one canonical structure. Gherkin, Excel and Jira are renderers over it: no format is second-class, and a fourth output means writing a renderer, not touching the pipeline. A recording produces an array of test cases (SS9.3).",
  "type": "object",

  "properties": {
    "schemaVersion": { "type": "string", "const": "1.0" },
    "recordingId": { "type": "string" },
    "runId": { "type": "string", "description": "Links to the agent trace." },
    "projectId": { "type": "string" },
    "ownerId": { "type": "string" },
    "createdAt": { "type": "string", "format": "date-time" },
    "testCases": {
      "type": "array",
      "items": { "$ref": "#/$defs/TestCaseIR" }
    }
  },
  "required": [
    "schemaVersion",
    "recordingId",
    "runId",
    "projectId",
    "ownerId",
    "createdAt",
    "testCases"
  ],
  "additionalProperties": false,

  "$defs": {
    "TestCaseIR": {
      "title": "TestCaseIR",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "recordingId": { "type": "string" },
        "runId": { "type": "string" },
        "kind": { "title": "TestCaseKind", "type": "string", "enum": ["test_case", "bug_report"] },

        "title": {
          "type": "string",
          "description": "The capability under test, e.g. 'Order approval'. Renders as the Gherkin Feature name. NOT the tester's objective string: a Feature that repeats the Scenario verbatim reads as machine output, which is the first thing a QA lead judges."
        },
        "description": {
          "type": "string",
          "description": "One or two sentences of context. Renders as the free-text description block under Feature:, which is where Gherkin natively puts this -- a leading # comment is not."
        },
        "scenarioName": {
          "type": "string",
          "description": "The specific case this test exercises, e.g. 'An order over EUR500 is held for manager approval'. Renders as the Scenario name. Falls back to `title` only when composition did not run."
        },
        "objective": {
          "type": "string",
          "description": "The tester's stated objective, verbatim (SS6.6)."
        },
        "preconditions": {
          "type": "array",
          "items": { "$ref": "#/$defs/Precondition" },
          "description": "From segments classified 'setup'. Lift into a Gherkin Background when shared across test cases."
        },
        "tags": { "type": "array", "items": { "type": "string" } },

        "steps": { "type": "array", "items": { "$ref": "#/$defs/Step" } },
        "parameters": {
          "type": "array",
          "items": { "$ref": "#/$defs/Parameter" },
          "description": "From redaction placeholders (SS7.2)."
        },

        "omitted": {
          "type": "array",
          "items": { "$ref": "#/$defs/OmittedSegment" },
          "description": "Exploratory/abandoned segments -- shown, not hidden. A verbatim transcript is unusable; silent deletion is untrustworthy."
        },
        "examples": {
          "title": "ScenarioExamples",
          "type": "object",
          "description": "A `Scenario Outline`'s table, when the author decided this flow is one behaviour exercised with several sets of values.\n\nDistinct from `parameters`, which lifts REDACTION placeholders into an Examples row and is a rendering setting. This is a judgement about test design: a recording that adds 13 items and then 18 comes out as two near-identical scenarios and reads as a transcript, where one outline over two rows reads as a test somebody designed.",
          "properties": {
            "columns": { "type": "array", "items": { "type": "string" } },
            "rows": {
              "type": "array",
              "items": { "type": "array", "items": { "type": "string" } }
            }
          },
          "required": ["columns", "rows"],
          "additionalProperties": false
        },
        "suggestions": {
          "type": "array",
          "items": { "$ref": "#/$defs/CoverageSuggestion" },
          "description": "SS9.8 -- STRICTLY QUARANTINED. Never rendered as steps, never exported as test cases, always labelled unverified."
        },

        "metadata": { "$ref": "#/$defs/TestCaseMetadata" },
        "warnings": {
          "type": "array",
          "items": { "$ref": "#/$defs/Warning" },
          "description": "Unresolved fidelity flags and critic findings. Never collapsed by default in the UI."
        },

        "bug": { "$ref": "#/$defs/BugDetail" }
      },
      "required": [
        "id",
        "recordingId",
        "runId",
        "kind",
        "title",
        "description",
        "preconditions",
        "tags",
        "steps",
        "parameters",
        "omitted",
        "metadata",
        "warnings"
      ],
      "additionalProperties": false
    },

    "Step": {
      "title": "Step",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "keyword": {
          "title": "StepKeyword",
          "type": "string",
          "enum": ["Given", "When", "Then", "And", "But"],
          "description": "The keyword the author WROTE, read back off the feature file it wrote.\n\nIt was derived from `role` plus position, which was right while the model was shown one segment at a time and could not know where the scenario turned -- asked for a keyword with no view of the flow it answered `When` every time, and Phase 1 shipped seven of them in a row. An author that writes the whole document in Gherkin has already decided, in the one place where the decision is visible to a reader, so deriving it a second time could only disagree with the file.\n\n`But` is here because the author's own parser has always accepted it and this enum did not, which made a perfectly ordinary negative step a validation error waiting for the first model that wrote one."
        },
        "text": { "type": "string", "description": "Intent, not mechanics. 'Submits the order' beats 'clicks the blue button'." },
        "role": {
          "$ref": "common.schema.json#/$defs/SegmentRole",
          "description": "SS9.3 -- what this step does in the narrative. Setup, the behaviour under test, or teardown. Kept alongside `keyword` because it survives re-rendering: a scenario that gets split still knows which of its steps were preconditions."
        },

        "eventIds": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Traceability into the recording. One of the two backlinks that carry the whole trust story (SS10): this one proves the sentence came from the recording.\n\nEmpty is legal, and only for a step that exists to CHECK something. An expected result is about what the application did and does not need an action of its own -- `Then the number of free rooms drops from 3 to 2` is a step nobody clicked, and inventing a click to hang it on would put a sentence in the feature file describing something that never happened. Such a step carries its backlink on the assertion's own `evidence.eventId` instead.\n\nThis was `minItems: 1` and the author's worked example showed a verdict-only step with no events, so the prompt taught a shape the schema rejected -- and it only surfaced the first time a real model took the example at its word, several stages downstream, as a Pydantic error during assembly. Worked examples outweigh rules; when the two disagree the example is usually right and the rule is the thing to change.\n\nThe net that matters is `event_coverage`, which requires every recorded event to land in exactly one step or in an explicit omission. That is a statement about events, not about steps, and a step with no events cannot violate it."
        },
        "investigationRef": {
          "type": "string",
          "description": "-> StepInvestigation in the trace. Renders as the 'why this step' panel (SS13.3)."
        },
        "screenshotRef": { "type": "string" },
        "selectorHints": {
          "type": "array",
          "items": { "$ref": "#/$defs/SelectorHint" },
          "description": "For later automation. Rendered as Gherkin comments, never in step text."
        },

        "assertions": { "type": "array", "items": { "$ref": "#/$defs/Assertion" } },
        "confidence": { "$ref": "common.schema.json#/$defs/Confidence" },
        "escalation": {
          "type": "string",
          "description": "A specific question for the human. A first-class outcome, not a failure -- an agent that asks is more useful than one that guesses."
        },
        "fidelity": {
          "type": "array",
          "items": { "$ref": "common.schema.json#/$defs/FidelityFlag" }
        },
        "whyNot": {
          "type": "string",
          "description": "Why this step has no expected result, in language the tester can act on: 'the product list was never captured before or after this click, so nothing here shows the order changed'.\n\nRefusal used to be something DONE TO the author -- a claim was proposed, could not be proved, and was deleted, so the scenario quietly ended with no `Then` while 27 style warnings said so in a vocabulary nobody outside the pipeline reads. It is now something the author WRITES, which means it can explain itself and a reviewer can close the gap. Never rendered into the feature body, which is prose and nothing else."
        },
        "criticNotes": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["id", "keyword", "text", "eventIds", "investigationRef", "assertions", "confidence", "fidelity"],
      "additionalProperties": false
    },

    "Assertion": {
      "title": "Assertion",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "text": {
          "type": "string",
          "description": "Prose -- free. 'the confirmation banner appears'. This is what defuses paraphrase thrash: the model is never forced to write stiff sentences to satisfy a string match."
        },
        "provenance": { "$ref": "common.schema.json#/$defs/Provenance" },
        "evidence": { "$ref": "#/$defs/Evidence" },
        "accepted": { "type": "boolean" },
        "rank": {
          "type": "integer",
          "description": "Candidate ordering within the step. Two or three where the step genuinely produced more than one checkable outcome, one where only one thing mattered, none where nothing observable happened (SS9.5). Forcing a second candidate onto a step with one obvious outcome manufactures exactly the weak incidental assertion the ranking exists to demote."
        }
      },
      "required": ["id", "text", "provenance", "evidence", "accepted"],
      "additionalProperties": false
    },

    "Evidence": {
      "title": "Evidence",
      "description": "SS3.2 -- the single most important structure in the system. A claim is valid only if its literal appeared in a tool response THIS agent actually received during THIS run.",
      "type": "object",
      "properties": {
        "literal": {
          "type": "string",
          "description": "Exact retrieved string, e.g. 'Order confirmed'. Checked character-for-character by evidence_retrieved."
        },
        "toolCallId": {
          "type": "string",
          "description": "The retrieval that produced it, e.g. tc_0447. The second of the two backlinks (SS10): this one proves the agent went and looked. If this id does not resolve in the trace, the assertion is rejected."
        },
        "eventId": { "type": "string" },
        "kind": {
          "title": "EvidenceKind",
          "type": "string",
          "enum": ["semantic_node", "url", "network", "console", "narration", "annotation", "a11y_node"]
        },
        "predicate": { "$ref": "#/$defs/Predicate" }
      },
      "required": ["literal", "toolCallId", "eventId", "kind"],
      "additionalProperties": false
    },

    "Predicate": {
      "title": "Predicate",
      "description": "WHAT is being claimed about the retrieval, not merely that a string is in it.\n\nWithout this the gate is substring containment, and `Then the first product is 'The Autumnal Hamper'` is proved by that string appearing ANYWHERE in the response. The sentence says FIRST; the check says PRESENT. Sorting, ranking, pagination and every negative assertion are inexpressible, so an author asked for a verdict on a sort could only ever restate that the page still says what the tester set the dropdown to.\n\nAbsent means `contains`, which is the historical behaviour, so an assertion written before this existed means exactly what it always did.\n\nThree things about the design are load-bearing and were each a way to ship this broken:\n\n1. A predicate addresses nodes by ROLE and NAME, never by a css id or a `ref`. The node model is ref/role/name/value/children and there are no ids anywhere; `ref` is a structural path that is stable only WITHIN one snapshot, so a predicate binds to exactly one stored response and cannot be re-pointed to another event the way a bare literal can.\n\n2. It is evaluated against the STORED response, which is the full one. `get_snapshot` renders a capped view to the model, and the ranking that cap uses puts named nodes first -- so `first_of` evaluated against the capped view would return the first NAMED node rather than the first in document order, and on a product grid the nameless wrapper nodes are exactly what ranks to the back. It would confidently return the wrong answer and pass the gate.\n\n3. It has THREE outcomes, not two. A predicate that cannot be evaluated at all -- container absent, role mismatch, the response is not a snapshot -- is neither true nor false, and treating it as either is a different failure. Pass builds a laundering machine that puts a green badge on an unchecked claim; reject silently kills true claims whenever a response shape changes. It goes to `whyNot`.",
      "type": "object",
      "properties": {
        "form": {
          "title": "PredicateForm",
          "type": "string",
          "enum": ["contains", "first_of", "count", "absent"],
          "description": "contains  -- the literal appears in the response. The default, and what every assertion written before this meant.\nfirst_of  -- the literal names the FIRST child of `container`, in document order. This is what makes a sort or a ranking checkable.\ncount     -- `container` holds exactly `n` children of role `role`. 'the list drops from 24 products to 9'.\nabsent    -- the literal does NOT appear. The judge's most frequent unmet ask, and the only form whose citation cannot be resolved by searching for its own literal."
        },
        "container": {
          "$ref": "#/$defs/NodeRef",
          "description": "Which part of the page the claim is about. Required by first_of and count; meaningless to contains and absent, which are about the whole response."
        },
        "role": {
          "type": "string",
          "description": "Which children `count` counts, e.g. 'listitem'. Absent counts every child."
        },
        "n": {
          "type": "integer",
          "minimum": 0,
          "description": "The expected count, for `count`."
        }
      },
      "required": ["form"],
      "additionalProperties": false
    },

    "NodeRef": {
      "title": "NodeRef",
      "description": "A node named the way the accessibility tree names it. Never a css selector: the recorder is black-box and reads the live accessibility tree, so role plus accessible name is the only address that exists for it -- and it is the address a human reading the test can also check.",
      "type": "object",
      "properties": {
        "role": { "type": "string" },
        "name": { "type": "string", "description": "The accessible name. Matched case-insensitively, and a container with no name is addressed by role alone." }
      },
      "required": ["role"],
      "additionalProperties": false
    },

    "Precondition": {
      "title": "Precondition",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "text": { "type": "string" },
        "eventIds": { "type": "array", "items": { "type": "string" } },
        "shared": {
          "type": "boolean",
          "description": "True when this precondition is common to several test cases and should lift into a Background block."
        }
      },
      "required": ["id", "text", "eventIds"],
      "additionalProperties": false
    },

    "Parameter": {
      "title": "Parameter",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "placeholder": { "type": "string" },
        "category": { "type": "string" },
        "description": { "type": "string" }
      },
      "required": ["name", "placeholder", "category"],
      "additionalProperties": false
    },

    "SelectorHint": {
      "title": "SelectorHint",
      "type": "object",
      "properties": {
        "strategy": { "title": "SelectorStrategy", "type": "string", "enum": ["testId", "role", "text", "css"] },
        "value": { "type": "string" },
        "stability": { "$ref": "common.schema.json#/$defs/Confidence" }
      },
      "required": ["strategy", "value", "stability"],
      "additionalProperties": false
    },

    "OmittedSegment": {
      "title": "OmittedSegment",
      "type": "object",
      "properties": {
        "segmentId": {
          "type": "string",
          "description": "The segment this omission came from, when a segment is what was omitted. Absent when the drafter omitted a set of events that does not correspond to one segment."
        },
        "eventIds": {
          "type": "array",
          "items": { "type": "string" },
          "description": "The events this omission accounts for. event_coverage is the net under the drafter's freedom to choose step boundaries: every recorded event must land in a step or in one of these, so an omission that names no events cannot discharge anything."
        },
        "reason": { "title": "OmissionReason", "type": "string", "enum": ["exploratory", "abandoned"] },
        "eventCount": { "type": "integer", "minimum": 1 },
        "summary": { "type": "string", "description": "e.g. 'browsed the reports page, returned'." },
        "afterStepId": {
          "type": "string",
          "description": "Which step this omission followed, so the UI can place the '3 exploratory actions omitted' marker."
        }
      },
      "required": ["reason", "eventCount", "summary"],
      "additionalProperties": false
    },

    "CoverageSuggestion": {
      "title": "CoverageSuggestion",
      "description": "SS9.8 -- a prompt for the tester, not an artifact. Must never contaminate grounded output.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "text": { "type": "string" },
        "rationale": { "type": "string" },
        "category": {
          "title": "SuggestionCategory",
          "type": "string",
          "enum": ["validation_path", "api_error_shape", "boundary_value", "disabled_state", "visible_branch"]
        },
        "basedOn": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Event ids or tool call ids the observation rests on. Unverified is not the same as ungrounded."
        }
      },
      "required": ["id", "text", "rationale", "category"],
      "additionalProperties": false
    },

    "Warning": {
      "title": "Warning",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "source": { "title": "WarningSource", "type": "string", "enum": ["fidelity", "critic", "validator", "recorder"] },
        "severity": { "title": "WarningSeverity", "type": "string", "enum": ["info", "warn", "error"] },
        "message": { "type": "string" },
        "stepId": { "type": "string" },
        "code": { "type": "string" }
      },
      "required": ["id", "source", "severity", "message"],
      "additionalProperties": false
    },

    "TestCaseMetadata": {
      "title": "TestCaseMetadata",
      "type": "object",
      "properties": {
        "capturedAt": { "type": "string", "format": "date-time" },
        "durationMs": { "type": "number" },
        "browser": { "type": "string" },
        "viewport": {
          "title": "Viewport",
          "type": "object",
          "properties": { "w": { "type": "integer" }, "h": { "type": "integer" } },
          "required": ["w", "h"],
          "additionalProperties": false
        },
        "startUrl": { "type": "string" },
        "projectId": { "type": "string" },
        "ownerId": { "type": "string" }
      },
      "required": ["capturedAt", "durationMs", "browser", "viewport", "startUrl", "projectId", "ownerId"],
      "additionalProperties": false
    },

    "BugDetail": {
      "title": "BugDetail",
      "description": "SS14.2 -- present only when kind is 'bug_report'. `expected` and `actual` are subject to the same evidence binding as any assertion: `actual` must quote something the agent retrieved.",
      "type": "object",
      "properties": {
        "failureStepId": { "type": "string" },
        "expected": { "type": "string" },
        "actual": { "type": "string" },
        "actualEvidence": { "$ref": "#/$defs/Evidence" },
        "consoleErrorIds": { "type": "array", "items": { "type": "string" } },
        "failedRequestIds": { "type": "array", "items": { "type": "string" } },
        "screenshotAtFailure": { "type": "string" },
        "environment": {
          "title": "BugEnvironment",
          "type": "object",
          "properties": {
            "browser": { "type": "string" },
            "viewport": { "type": "string" },
            "url": { "type": "string" }
          },
          "required": ["browser", "viewport", "url"],
          "additionalProperties": false
        }
      },
      "required": ["failureStepId", "expected", "actual", "environment"],
      "additionalProperties": false
    }
  }
}
